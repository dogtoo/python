<!DOCTYPE html>

<html lang="zh-Hant-TW">
<head>
    <meta http-equiv="content-type" content="text/html;charset=UTF8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title></title>
    <script src="js/util.js" type="text/javascript" charset="UTF8"></script>
    <script src="js/absPainter.js" type="text/javascript" charset="UTF8"></script>
    <script src="js/axis-x.js" type="text/javascript" charset="UTF8"></script>
    <script src="js/axis-y.js" type="text/javascript" charset="UTF8"></script>
    <script src="js/k-line.js" type="text/javascript" charset="UTF8"></script>
    <script src="js/line.js" type="text/javascript" charset="UTF8"></script>
    <script src="js/k-data.js" type="text/javascript" charset="UTF8"></script>
</head>
<body>
    <h1 id="h1"><%=msg%></h1>
    <canvas id="kline" width="1002" height="500" style="z-index: 2; border: 1px solid #69c"></canvas>
</body>
<script type="text/javascript">

    var painterKline = function(canvasId, paintStream, data) {
        Painter.call(this, canvasId, paintStream);
        this.data = data;
    };
    painterKline.prototype = Object.create(Painter.prototype);
    painterKline.prototype.constructor = painterKline;
    painterKline.prototype.preData = function() {
        //this.data = getKLData();
    }

    function MACount(maType, date, data) {
        maType = maType - 1;
        var MA = 0;
        if (maType > 50)
            console.log(maType + "date:" + date);
        if (date > maType) {
            for (var ma = date-maType-1; ma <= date-1; ma++)
                MA = MA + data[ma][5];
            return [data[ma][0], (MA / (maType+1))];
        } else {
            return [null, null];
        }
    }

{
    var fline = new line({
                yAxis: 'priceY',
                xAxis: 'dateX',
                strokeStyle: 'cyan',
                lineWidth: 1,
                disX: 5
            });
    //fline.__proto__ = Object.create(line.__proto__);
    fline.__proto__.preData =  function(data) {
        fline.options.disX = painter.implements.kline.options.lineWidth;
    }

    var tline = new line({
                yAxis: 'priceY',
                xAxis: 'dateX',
                strokeStyle: 'limegreen',
                lineWidth: 1,
                disX: 5
            });
    tline.__proto__.preData =  function(data) {
        tline.options.disX = painter.implements.kline.options.lineWidth;
    }

    var sline = new line({
                yAxis: 'priceY',
                xAxis: 'dateX',
                strokeStyle: 'magenta',
                lineWidth: 1,
                disX: 5
            });
    sline.__proto__.preData =  function(data) {
        sline.options.disX = painter.implements.kline.options.lineWidth;
    }

    var paintStream = [
        {
            'price':{
                region: { x: 'padding.left', y: 'padding.top', width: 'f:00.00', height: '55%' },
                'priceY':new yAxis({
                    font: '11px Arial',
                    color: 'black',
                    align: 'right',
                    textBaseline: 'top',
                    dataType: 'money',
                    dataRange: 'range'
                })
            }
        },
        {
            'main':{
                region: {
                    x: {'$and':['price.x', 'price.width', 3]}, y: {'$and':['price.y', (determineFontHeight('11px Arial')/2)]}
                    , width: {'$sub':['100%','price.width','padding.right','padding.left']}, height: '55%',
                    //horizontalLineCount: 7, verticalLineCount: 7,
                    //backgroundColor:'#fff',
                    riseColor: 'red',
                    fallColor: 'green',
                    normalColor: 'black',
                    chartMargin:{left:45.5,top:20.5,right:20.5},
                    barWidth: 5, spaceWidth: 2, lineStyle: 'solid', borderColor: 'gray', splitLineColor: '#eeeeee', lineWidth: 1
                },
                'kline':new kLine({
                    lineWidth: 1,
                    lineStyle: 'solid',
                    yAxis: 'priceY',
                    xAxis: 'dateX'
                }),
                'fline': fline,
                'tline': tline,
                'sline': sline
            }
        },
        {
            'date':{
                region: { x: 'main.x', y: {'$and':['main.height', 'main.y', determineFontHeight('11px Arial')/2]}, width: 'main.width', height: 'f:00-00-00' },
                'dateX':new xAxis({
                    font: '11px Arial',
                    color: 'black',
                    align: 'right',
                    textBaseline: 'top',
                    dataType: 'date',
                    dataRange: 'seq'
                })
            }
        },
        {
            'volume':{
                region: { x: 'price.x', y: {'$and':['price.height', {'$mul':['date.height',2]}]}, width: 'f:0000', height: '10%' },
                'volumeY':new yAxis({
                    font: '11px Arial',
                    color: 'black',
                    align: 'right',
                    textBaseline: 'top',
                    dataType: 'number',
                    dataRange: 'seq'
                })
            }
        }
    ];

    var painter = new painterKline('kline',paintStream, getKLData());
    painterKline.prototype.preData = function() {
        //this.data.priceY = [];
        var kline = [];

        var fline = [];
        var tline = [];
        var sline = [];

        var priceYL = [];
        var dateXL = [];
        var priceY = {};
        var dateX = {};
        for (var i =　140; i < 150; i++) {
            var rawData = data[i];
            priceY[Number(rawData[3])] = 1;
            priceY[Number(rawData[4])] = 1;
            dateX[rawData[0]] = 1;
            kline.push([rawData[0], rawData[2], rawData[5], rawData[3], rawData[4] ]);

            fline.push(MACount(5, i, data));
            tline.push(MACount(10, i, data));
            sline.push(MACount(20, i, data));
        }
        this.data.kline = kline;
        this.data.fline = fline;
        this.data.tline = tline;
        this.data.sline = sline;

        for (var pk in priceY)
            priceYL.push(pk);

        priceYL.sort(function(a, b) {
            return a - b;
        });
        this.data.priceY = priceYL;
        //Object.keys(priceY).reduce((a, c) => (a[c] = priceY[c], a), {});

        for (var dk in dateX)
            dateXL.push(dk);

        dateXL.sort(function(a, b) {
            return a - b;
        });
        this.data.dateX = dateXL;

        this.data.volumeY = ["0", "1000","2000"];
    }

    painterKline.prototype.start = function () {
        this.ctx.beginPath();
        this.ctx.strokeStyle = 'gray';
        this.ctx.save();
        this.ctx.translate(this.paintRegion['main.x']
                       , this.paintRegion['main.y']);

        this.ctx.rect(0, 0
                       , this.paintRegion['main.width']
                       , this.paintRegion['main.height']);
        this.ctx.stroke();
        var spaceHeight = 0;
        //console.log('height:'+this.paintRegion['price.height']+',length:'+(this.implements.priceY.options.horizontalLineCount - 1)+',fontHeight:'+this.implements.priceY.options.fontHeight / 2);
        /*
        for (var i = 1; i <= (this.implements.priceY.options.horizontalLineCount-2); i++) {
            spaceHeight = this.paintRegion['price.height'] * i / (this.implements.priceY.options.horizontalLineCount - 1) - (this.implements.priceY.options.fontHeight/2);
            var y = spaceHeight;
            this.drawHLine('#eeeeee', 0, y, this.paintRegion['main.width'], 1, 'solid');
        }*/

        for (var i in this.implements.priceY.options.y) {
            this.drawHLine('#eeeeee', 0, this.implements.priceY.options.y[i], this.paintRegion['main.width'], 1, 'solid');
        }
        /*
        console.log('lineWidth:'+this.implements.kline.options.lineWidth);
        console.log('width:'+this.paintRegion['main.width']+',Count:'+this.implements.dateX.options.verticalLineCount);
        var spaceWidth;
        for (var i = 1; i <= this.implements.dateX.options.verticalLineCount-2; i++) {
            spaceWidth = (this.paintRegion['main.width'] * i) / (this.implements.dateX.options.verticalLineCount - 1) - this.implements.kline.options.lineWidth / 2;
            var w = spaceWidth ;
            //if (w * 10 % 10 == 0) w += .5;
            this.drawVLine('#eeeeee', w, 0,this.paintRegion['main.height'], 1, 'solid');
        }*/
            var v_c = Math.round((this.implements.dateX.options.x.length)/this.implements.dateX.options.verticalLineCount);
            for (var i = 0 ; i < this.implements.dateX.options.x.length ; i+= v_c) {
                var diff = this.implements.dateX.options.x.length - i;
                if (diff < v_c) i = this.implements.dateX.options.x.length;
                this.drawVLine('#eeeeee', this.implements.dateX.options.x[i], 0,this.paintRegion['main.height'], 1, 'solid');
            }
        this.ctx.restore();
    }
}


/*
{
    var fline = new line({
                yAxis: 'dateY',
                xAxis: 'priceX',
                strokeStyle: 'cyan',
                lineWidth: 0.5,
                disY: 5
            });
    //fline.__proto__ = Object.create(line.__proto__);
    fline.__proto__.preData =  function(data) {
        fline.options.disY = painter.implements.kline.options.lineHigh/2;
    }

    var tline = new line({
                yAxis: 'dateY',
                xAxis: 'priceX',
                strokeStyle: 'limegreen',
                lineWidth: 1,
                disY: 5
            });
    tline.__proto__.preData =  function(data) {
        tline.options.disY = painter.implements.kline.options.lineWidth/2;
    }

    var sline = new line({
                yAxis: 'dateY',
                xAxis: 'priceX',
                strokeStyle: 'magenta',
                lineWidth: 1,
                disY: 5
            });
    sline.__proto__.preData =  function(data) {
        sline.options.disY = painter.implements.kline.options.lineWidth/2;
    }

    var paintStream = [
        {
            'date':{
                region: { x: 'padding.left', y: 'padding.top', width: 'f:00-00-00', height: '50%' },
                'dateY':new yAxis({
                    font: '11px Arial',
                    color: 'black',
                    align: 'right',
                    textBaseline: 'top',
                    dataType: 'string'
                })
            }
        },
        {
            'main':{
                region: {
                    x: {'$and':['date.x', 'date.width', 3]}, y: {'$and':['date.y', (determineFontHeight('11px Arial')/2)]}
                    , width: {'$sub':['100%','date.width','padding.right','padding.left']}, height: {'$sub':['50%', determineFontHeight('11px Arial')]},
                    //horizontalLineCount: 7, verticalLineCount: 7,
                    //backgroundColor:'#fff',
                    riseColor: 'red',
                    fallColor: 'green',
                    normalColor: 'black',
                    chartMargin:{left:45.5,top:20.5,right:20.5},
                    barWidth: 5, spaceWidth: 2, lineStyle: 'solid', borderColor: 'gray', splitLineColor: '#eeeeee', lineWidth: 1
                },
                'kline':new kLine({
                    lineWidth: 1,
                    lineStyle: 'solid',
                    borderColor: 'gray',
                    yAxis: 'dateY',
                    xAxis: 'priceX'
                }),
                'fline': fline,
                'tline': tline,
                'sline': sline
            }
        },
        {
            'price':{
                region: { x: 'main.x', y: {'$and':['main.height', 'main.y', (determineFontHeight('11px Arial')/2)]}, width: 'main.width', height: 'f:00.00' },
                'priceX':new xAxis({
                    font: '11px Arial',
                    color: 'black',
                    align: 'right',
                    textBaseline: 'top',
                    dataType: 'money'
                })
            }
        }
    ];

    var painter = new painterKline('kline',paintStream, getKLData());
    painterKline.prototype.preData = function() {
        var kline = [];

        var fline = [];
        var tline = [];
        var sline = [];

        var priceXL = [];
        var dateYL = [];
        var priceX = {};
        var dateY = {};
        for (var i = 100; i < 150; i++) {
            var rawData = data[i];
            //20111215,11.68,11.65,11.76,11.40,11.41,43356655,502325991
            priceX[Number(rawData[3])] = 1;
            priceX[Number(rawData[4])] = 1;
            dateY[rawData[0]] = 1;
            kline.push([rawData[0], rawData[2], rawData[5], rawData[3], rawData[4] ]);

            fline.push(MACount(5, i, data));
            tline.push(MACount(10, i, data));
            sline.push(MACount(20, i, data));
        }
        this.data.kline = kline;
        this.data.fline = fline;
        this.data.tline = tline;
        this.data.sline = sline;

        for (var pk in priceX)
            priceXL.push(pk);

        priceXL.sort(function(a, b) {
            return a - b;
        });
        this.data.priceX = priceXL;
        //Object.keys(priceY).reduce((a, c) => (a[c] = priceY[c], a), {});

        for (var dk in dateY)
            dateYL.push(dk);

        dateYL.sort(function(a, b) {
            return a - b;
        });
        this.data.dateY = dateYL;
    }

    painterKline.prototype.start = function () {
        this.ctx.beginPath();
        this.ctx.strokeStyle = 'gray';
        this.ctx.save();
        this.ctx.translate(this.paintRegion['main.x']
                       , this.paintRegion['main.y']);

        this.ctx.rect(0, 0
                       , this.paintRegion['main.width']
                       , this.paintRegion['main.height']);
        this.ctx.stroke();
        var spaceHeight = (this.paintRegion['main.height'] - (this.implements.dateY.options.fontHeight/2)) / (this.implements.dateY.options.horizontalLineCount - 1);
        for (var i = 1; i <= (this.implements.dateY.options.horizontalLineCount-2); i++) {
            var y = spaceHeight * i;
            this.drawHLine('#eeeeee', 0, y, this.paintRegion['main.width'], 1, 'solid');
        }

        var spaceWidth = this.paintRegion['main.width'] / (this.implements.priceX.options.verticalLineCount - 1);
        for (var i = 1; i <= this.implements.priceX.options.verticalLineCount; i++) {
            var w = spaceWidth * i;
            if (w * 10 % 10 == 0) w += .5;
            this.drawVLine('#eeeeee', w, 0,this.paintRegion['main.height'], 1, 'solid');
        }

        this.ctx.restore();
    }
}
*/
    painter.padding.top = 10;
    painter.padding.left = 10;
    painter.padding.right = 20;
    painter.init();



    painter.build();
painter.build();
</script>
</html>
